DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=intrarp

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Optional: GitHub Personal Access Token for update checks
# Helps avoid API rate limits (60 req/hour without token, 5000 req/hour with token)
# Create token at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=